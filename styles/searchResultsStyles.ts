import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  body: {
    marginTop: 3,
    marginHorizontal: 15,
    gap: 15,
    paddingBottom: 15,
  },
  emptyState: {
    marginTop: 100,
    paddingHorizontal: 30,
  },
});
