import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fffbfe",
  },
  body: {
    marginTop: 3,
    marginHorizontal: 15,
    gap: 15,
    paddingBottom: 15,
  },
});
