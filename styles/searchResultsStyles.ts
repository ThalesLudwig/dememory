import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  body: {
    marginTop: 3,
    gap: 2,
    paddingBottom: 15,
  },
  emptyState: {
    marginTop: 100,
    paddingHorizontal: 30,
  },
});
