import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  body: {
    marginTop: 60,
    marginHorizontal: 15,
    gap: 15,
    paddingBottom: 15,
  },
  wallet: {
    marginTop: 15,
  },
  avatar: {
    justifyContent: "center",
    alignItems: "center",
  },
});
